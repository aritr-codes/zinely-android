# Zinely 0.9.0-beta.4-r2 - tester package

## Zinely - smaller beta 4 maintenance revision

This is a packaging-only update. Zinely's workflows, document format, offline behavior, rendering,
accessibility, and saved zines are unchanged from beta.4-r1. The APK is smaller because release-only
shrinking removes unreachable code/resources and the unchanged launcher collage is packaged as lossless WebP.

## Before installing

This APK uses the same Zinely release key as beta.1 through beta.4-r1, so Android can update an existing
release-signed beta without deleting its data. **Do not uninstall the existing app.** Make a fresh backup
from **Backups** before any beta update.

1. Download `zinely-0.9.0-beta.4-r2-release.apk` to the phone.
2. Tap it, allow that file-opening app to install unknown apps if Android asks, then return to the installer.
3. Choose **Update** / **Install**, then open Zinely normally.

Zinely requires Android 7.0 or newer. It has no account, analytics, cloud sync, or network permission.

## What to verify

1. Existing zines and backups remain reachable.
2. Add Photo, Replace Photo, and Art Replace open their expected destinations.
3. Text and emoji appear in the editor, PNG output, and PDF output.
4. Read, Save PDF, Open PDF, and Print & fold remain reachable.
5. The launcher icon and brief system splash retain the same collage and central Z.

## Known limitations

- Android owns the photo picker and may show an additional Preview / Done step.
- Replaced and deleted photo assets are retained, so app storage can grow over time.
- Font choice remains unavailable. Unsupported print scripts are kept and warned about but do not print.
- Zinely saves the PDF; printing happens in the phone's PDF or print app.
- Privacy-policy publication remains blocked on owner-provided details.

---

## Release metadata

| | |
|---|---|
| Version name | `0.9.0-beta.4-r2` |
| `versionCode` | 8 |
| Artifact | `zinely-0.9.0-beta.4-r2-release.apk` - 16,052,043 bytes (15.31 MiB) |
| SHA-256 | `1dab5b6dd0ad60b3b00af73432d62caf1e10736497e52e1845449c797f6fd417` |
| Size change from beta.4-r1 | 13,021,526 bytes smaller; 44.8% reduction |
| Signing | Zinely release key (`CN=Zinely, O=Zinely, C=IN`) |
| Signing certificate SHA-256 | `8ade47cf70c18b5e6f4901c89fdab97d40a2d17e4c5acf33e0e1f0b6de469b36` |
| Min / target Android | 7.0 (API 24) / API 36 |
| Source commit | `98be246` (`chore(release): prepare smaller beta 4 r2 package`) |
| Tag | not created; requires explicit authorization after release acceptance |

## Size evidence

| Packaged group | beta.4-r1 | beta.4-r2 | Result |
|---|---:|---:|---|
| DEX | 13.23 MiB | 1.80 MiB | R8 removed unreachable code and consolidated five DEX files into two |
| Assets | 10.21 MiB | 10.20 MiB | Offline emoji and render fonts preserved |
| Resources | 3.62 MiB | 3.01 MiB | Resource shrinking plus lossless WebP launcher projection |

The generated adaptive/splash artwork is 1,979,654 bytes rather than the prior 2,675,149-byte PNG.
The generator decoded the WebP and compared it with the immutable `APP_LOGO.png` master; every RGB pixel
matched. The bundled `NotoColorEmojiCompat.ttf` remains unchanged because it is part of Zinely's accepted
offline editor/PNG/PDF rendering contract.

## Verification

- The focused launcher-resource test, complete Gradle test suite, and pinned golden workflow passed.
- The golden workflow executed all 109 required tasks and produced no tracked golden changes.
- `:app:lintDebug`, `:app:assembleDebug`, and release-signed `:app:assembleRelease` passed.
- A second release build reported R8, resource optimization, packaging, and assembly as `UP-TO-DATE`,
  tying the copied APK to source commit `98be246`.
- APK inspection confirmed package `com.aritr.zinely`, versionCode 8, versionName
  `0.9.0-beta.4-r2`, minSdk 24, targetSdk 36, and no Internet permission.
- Signature inspection confirmed APK Signature Scheme v2 and the same Zinely release certificate as
  beta.4-r1.
- An isolated Android 16 emulator accepted a clean beta.4-r2 installation and cold-launched
  `MainActivity`. It also accepted an in-place beta.4-r1 to beta.4-r2 update: `firstInstallTime` remained
  `2026-08-31 17:18:29`, `lastUpdateTime` advanced to `2026-08-31 17:19:23`, and the updated app cold-launched.
- No physical device was attached during final package verification. The Samsung installation and settings
  were therefore not changed.

Independent review found no required code, architecture, visual-contract, privacy, or release-record fix and
independently reproduced the artifact metadata and archive-level size reduction. Its decision is **GO WITH
FIXES** until one short Samsung pass checks the exact r2 APK's launcher mask, cold/warm system-splash handoff,
and share-in entry. This is the only remaining release-acceptance gate; it does not invalidate the verified APK.

Changelog: [CHANGELOG.md](../../CHANGELOG.md#090-beta4-r2--2026-08-31--smaller-beta-4-maintenance-package) -
Previous package: [beta.4-r1](0.9.0-beta.4-r1.md) -
Mechanics: [RELEASING.md](../RELEASING.md)
